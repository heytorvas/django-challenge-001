from django.urls import path

from author import views

urlpatterns = [
    # path("",views.ListAuthorAPIView.as_view(),name="author_list"),
    # path("", views.CreateAuthorAPIView.as_view(),name="author_create"),
    # path("<int:pk>/",views.UpdateAuthorAPIView.as_view(),name="author_update"),
    # path("<int:pk>/",views.DeleteAuthorAPIView.as_view(),name="author_delete")

    #path("",views.AuthorViewSet.as_view(),name="author_view"),
]